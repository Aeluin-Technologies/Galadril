# Pipeline Configuration

Service settings live in `examples/connectors.yaml`. The standalone DAG in
`examples/pipeline.example.yaml` is an explicit example. Production tenant
pipelines are authored and published in TerminusDB through Gateway.

See [Pipeline storage and revision control](pipeline_storage.md) for defaults,
production deployment, tenant isolation, and migration from S3.
