# Bazel reads dependency metadata from uv.lock. This projection intentionally
# omits dependency groups so the complete uv workspace graph is exposed.
[project]
name = "galadril"
version = "0.1.0"
dependencies = ["testcontainers[kafka,postgres]>=4.0"]
