apiVersion: v1
kind: ConfigMap
metadata:
  name: registry
data:
  REGISTRY_BIND_ADDR: 0.0.0.0:50052
  REGISTRY_CONFIG_PATH: /etc/galadril/connectors.yaml
  LAKEFS_ENDPOINT: http://lakefs:8000
  REGISTRY_STORAGE_NAMESPACE: s3://galadril-registry/
  REGISTRY_REPOSITORY_PREFIX: galadril
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: registry
spec:
  # Writes are serialized per tenant in-process; scale after distributed
  # branch locking is introduced at the lakeFS transaction boundary.
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: registry
  template:
    metadata:
      labels:
        app.kubernetes.io/name: registry
    spec:
      containers:
        - name: registry
          image: ghcr.io/aeluin-technologies/galadril-registry:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: grpc
              containerPort: 50052
          envFrom:
            - configMapRef:
                name: registry
          env:
            - name: LAKEFS_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: registry-lakefs
                  key: access-key-id
            - name: LAKEFS_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: registry-lakefs
                  key: secret-access-key
          volumeMounts:
            - name: connectors
              mountPath: /etc/galadril
              readOnly: true
          readinessProbe:
            grpc:
              port: 50052
          livenessProbe:
            grpc:
              port: 50052
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            readOnlyRootFilesystem: true
            runAsNonRoot: true
      volumes:
        - name: connectors
          secret:
            secretName: galadril-connectors
---
apiVersion: v1
kind: Service
metadata:
  name: registry
spec:
  selector:
    app.kubernetes.io/name: registry
  ports:
    - name: grpc
      port: 50052
      targetPort: grpc
